// Write a program to swap three integer variable values. (a to b, b to c, c to a)

#include <stdio.h>
#include <stdlib.h>
#include <teachingcodes.h>

int main() {

	/* int a, b, c, d;

	a = 1;
	b = 2;
	c = 3;

	d = a;
	a = b;
	b = c;
	c = d;

	printf ("%d\n%d\n%d", a, b, c); */

	int a, b, c;

	a = 1;
	b = 2;
	c = 3;

	a = a + b;
	b = a - b; // b = a
	a = a - b; // a = b (+)
	b = b + c; // b = a + c
	c = b - c; // c = a (+)
	b = b - c; // b = c (+)

	printf ("%d\n%d\n%d", a, b, c);

	return 0;
}
