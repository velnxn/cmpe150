#include <stdio.h>
#include <stdlib.h>
#include <teachingcodes.h>

int main() {

	int x = 5;
	printf ("%d", x <= 10);

	/* Tried if =< would work and just slow it down because it
	 * would check if it's equal first every time but it gave a
	 * syntax error */

	return 0;
}
